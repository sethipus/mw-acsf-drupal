project:
  machine_name: mars
  prefix: MARS
  human_name: Mars
  profile:
    name: lightning
  local:
    protocol: http
    hostname: mars.ddev.site:8080
    # hostname: local.mars.com
git:
  default_branch: master
  remotes:
    - 'mars@svn-2398.enterprise-g1.hosting.acquia.com:mars.git'
deploy:
  tag_source: true
drush:
  aliases:
    remote: '${project.machine_name}.test'
    local: self
    ci: self
  default_alias: '${drush.aliases.local}'
validate:
  acsf: true
