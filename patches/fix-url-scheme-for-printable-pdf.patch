diff --git a/modules/printable_pdf/printable_pdf.info.yml b/modules/printable_pdf/printable_pdf.info.yml
index 9abc6e8..c093e53 100644
--- a/modules/printable_pdf/printable_pdf.info.yml
+++ b/modules/printable_pdf/printable_pdf.info.yml
@@ -3,6 +3,7 @@ type: module
 description: 'Provides a printable format for generating PDFs.'
 package: Printable
 core: 8.x
+core_version_requirement: ^8 || ^9
 dependencies:
   - printable:printable
   - pdf_api:pdf_api
diff --git a/printable.module b/printable.module
index e4b3da2..4f4b7e1 100755
--- a/printable.module
+++ b/printable.module
@@ -263,34 +263,6 @@ function printable_preparing_pdf() {
   );
 }

-/**
- * Make file urls absolute if we're generating a PDF.
- *
- * @param string $uri
- *   URI to be altered.
- */
-function printable_file_url_alter(&$uri) {
-  if (printable_preparing_pdf()) {
-    $scheme = \Drupal::service('file_system')->uriScheme($uri);
-
-    if ($scheme) {
-      $path = \Drupal::service('file_system')->realpath($scheme . '://');
-      $uri = ($path ? $path . '/' : '') . substr($uri, strlen($scheme) + 3);
-    }
-    else {
-      $base_url = \Drupal::request()->getSchemeAndHttpHost();
-
-      if (substr($uri, 0, strlen($base_url)) == $base_url) {
-        $uri = substr($uri, strlen($base_url) + 1);
-      }
-
-      $uri = DRUPAL_ROOT . '/' . $uri;
-    }
-
-    $uri = 'printable://' . $uri;
-  }
-}
-
 /**
  * Make Link URLs absolute if we're generating a PDF.
  *
