{#
/**
 * @file
 * Template for the salesforce form component.
 *
 * Available variables:
 * 'form_url' => NULL
 * 'accessibility_title' => NULL
 */
#}
{{ attach_library('emulsifymars/iframe') }}

{% include "@atoms/iframe/iframe.twig" with {
  theme_styles: 'drupal',
  iframe_src: form_url,
  iframe_description: accessibility_title,
  iframe_base_class: 'newsletter-iframe',
} %}
