{% set homepage_hero__base_class = homepage_hero__base_class|default('homepage-hero-video') %}
{% if dark_overlay %}
  {% set homepage_hero__modifiers = ['dark-overlay'] %}
{% endif %}

<section {{ bem(homepage_hero__base_class) }} data-theme="{{ theme_styles }}"
  {% if title_label %}
    data-component-title="{{ title_label }}"
  {% endif %}
>

  <div {{ bem('wrapper', [], homepage_hero__base_class, homepage_hero__modifiers) }}>
    <div {{ bem('container', ['title'], homepage_hero__base_class) }}>
      {% include "@atoms/text/eyebrow/eyebrow.twig" with {
        eyebrow_text: eyebrow
      } %}

      {% include "@atoms/text/headings/_heading_next_line.twig" with {
        heading_level: 2,
        heading: title_label,
        heading_override: title_label_override,
        heading_base_class:'heading',
        heading_url: title_href
      } %}

      {% include "@atoms/links/defaultLink/defaultLink.twig" with {
        default_link_content: cta_title,
        default_link_attributes: cta_link_attributes
      } %}
    </div>
    <div {{ bem('container', ['video'], homepage_hero__base_class) }}>
      {% include "@atoms/video/fullscreen-video/video.twig" with {
        video__src__url: video__src__url,
        video__control: true,
      } %}
    </div>
  </div>
</section>



