{% set product_content_pair_up_base_class = 'product-content-pair-up' %}
{% if dark_overlay %}
  {% set product_content_pair_up__modifiers = ['dark-overlay'] %}
{% endif %}

<section class="{{ product_content_pair_up_base_class }}" data-theme="{{ theme_styles }}">

  {% if title %}
    {% include "@atoms/text/headings/_heading.twig" with {
      heading_level: 2,
      heading: title,
      heading_base_class: product_content_pair_up_base_class ~ '__heading'
    }
    %}

    {% include "@atoms/separators/graphic-divider/graphic-divider.twig" with {
      graphic_divider_blockname: product_content_pair_up_base_class,
      graphic_divider: graphic_divider
    } %}
  {% endif %}

  <div {{ bem('container', [], product_content_pair_up_base_class, product_content_pair_up__modifiers) }}>
    <div class="lead-card" style="background-image: url('{{ background }}')">
      <div class="bg-image">
        {% set images = {
          1440: background|resizeByHeight(750),
          768: background|resizeByHeight(1003),
          375: background|resizeByHeight(845),
        } %}
        {% include "@atoms/images/image/responsive-image.twig" with {
          output_image_tag: true,
          image_alt: background,
          image_src: background|resizeByHeight(750),
          image_array: images,
          width: '1440px',
          height: '750px',
          image_base_class: 'lead-card-bg',
        } %}
      </div>
      <div class="wrapper">
        <div class="content-pair-up-eyebrow">
          {% include "@atoms/text/eyebrow/eyebrow.twig" with {
            eyebrow_text: lead_card_eyebrow
          } %}
        </div>
        {% include "@atoms/text/headings/_heading.twig" with {
          heading_level: 3,
          heading: lead_card_title,
          heading_url: cta_link_url,
          heading_base_class: 'heading'
        }
        %}
        {% include "@atoms/links/defaultLink/defaultLink.twig" with {
          default_link_content: cta_link_text|default('Explore'|mt),
          default_link_attributes: {
            href: cta_link_url
          }
        } %}
      </div>

      <div class="supportive-card">
        <div class="wrapper">
          {{ supporting_card_content }}
        </div>
      </div>
    </div>
  </div>
</section>
