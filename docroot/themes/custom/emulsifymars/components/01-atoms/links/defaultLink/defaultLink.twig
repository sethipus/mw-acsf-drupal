{# 
  Available variables:
    default_link_base_class - base class of the link
    default_link_border_radius - defines border radius in pixels
    default_link_attributes - attributes array, like href, target, rel, ...
    default_link_content - content of the link, image or text
 #}

{% set default_link_base_class = default_link_base_class|default('default-link') %}
{% set default_link_border_radius = default_link_border_radius|default(0) %}

<a 
  {{ bem(default_link_base_class) }}
  style="{{ 'border-radius: ' ~ default_link_border_radius ~ 'px' }}"
  {% for key, value in default_link_attributes %}
    {{ key }}="{{ value }}"
  {% endfor %}
>
  {% block default_link_content %}
    <span class="{{ default_link_base_class ~ '__label' }}">{{ default_link_content }}</span>
  {% endblock %}
</a>